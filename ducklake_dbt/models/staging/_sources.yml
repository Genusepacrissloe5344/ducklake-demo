version: 2

sources:
  - name: raw
    database: raw
    schema: "{{ 'raw_data' if target.name == 'sp' else 'main' }}"
    tables:
      - name: users
      - name: events
      - name: invoices
      - name: support_tickets
